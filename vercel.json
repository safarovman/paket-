{
  "version": 2,
  "buildCommand": "cd gboost-web && npm install && npm run build",
  "outputDirectory": "gboost-web/.next",
  "installCommand": "cd gboost-web && npm install",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
